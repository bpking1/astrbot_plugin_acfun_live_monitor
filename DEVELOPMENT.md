# Tracker 共享放映室监控

保留原 AcFun 监控及通知目标，新增独立 Tracker 轮询任务；任何一端配置错误或不可达不阻塞另一端。沿用现有 AstrBot HTTP 会话、消息 API 和插件启动/卸载生命周期。

配置包含开关、Tracker API 地址、观看页链接、片单媒体名和可选媒体 ID。API 地址是 Tracker，不是 SyncTV；不需要 SyncTV 密码。按根片单精确查找指定直播，遍历分页，重名或缺失时报错；配置 ID 则直接查询。只支持 SyncTV 自建推流的实时状态，不通过片单条目存在、房间播放状态或下载直播流判断开播。

请求 Tracker `/api/synctv/live-status?mediaId=...`，读取 active 和 startedAt。每分钟查询，首次成功仅记录基线；开播和新场次通知附观看链接，下播通知也保留链接。请求失败不当成下播；发送失败不推进状态，下次轮询重试。取消任务时释放资源。只编写单元及接口测试，不做端到端测试，不向真实会话发送测试消息。
