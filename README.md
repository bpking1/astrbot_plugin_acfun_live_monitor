# AcFun 直播监控

这是一个独立的极简 AstrBot 插件，只监控一个 AcFun 直播间，只在检测到新的开播时推送一次通知。通知包含标题、直播间地址和直播封面。

在 AstrBot WebUI 的「插件」页面打开本插件设置，填写两个配置项：

- `room_url`：完整直播间地址，例如 `https://live.acfun.cn/live/52002191`。
- `push_target`：目标会话的 UMO。在目标群聊或私聊发送 `/sid`，复制返回的 `UMO` 字段。

保存设置后重新加载插件。插件首次启动或更换直播间时只记录当前状态，不补发正在进行中的直播；之后检测到开播或新的直播场次才推送。
